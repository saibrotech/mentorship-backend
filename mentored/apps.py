"""Configuration file of mentored App."""

from django.apps import AppConfig


class MentoredConfig(AppConfig):
    """Configuration of mentored App."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'mentored'
